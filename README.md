Mail Sender Scheduler – Spring Boot (Beginner Friendly)

This project is a Spring Boot full-stack mail system that automatically sends an email to a user every 10 seconds, and also logs every sent email into a MySQL database.

It includes:

Automatic mail sending using Spring Scheduler

Email delivery using JavaMailSender

Data storage using Spring Data JPA

MySQL integration

Clean, simple structure for beginners
